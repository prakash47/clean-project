{
  "extends": "next/core-web-vitals",
  "rules": {
    // Add custom rules if needed
  },
  "overrides": [
    {
      "files": ["*.js", "*.jsx", "*.ts", "*.tsx"],
      "rules": {
        // Add specific rules for these file types if needed
      }
    }
  ]
}